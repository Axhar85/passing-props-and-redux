import React from "react";
import Section from "./Section";

const Main = (props) => (
    
    <div>
        <h3> This Main.js doing some cool stuff internall</h3>
        <hr/>
        {/*<Section name={props.name} age={props.age}  getOld= {props.getOld} />*/}
        <Section />
        
    </div>
);

export default Main;